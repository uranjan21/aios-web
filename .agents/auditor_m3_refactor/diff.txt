diff --git a/frontend/src/components/areas/finance/AccountsTab.tsx b/frontend/src/components/areas/finance/AccountsTab.tsx
index e4051c9..e761cb6 100644
--- a/frontend/src/components/areas/finance/AccountsTab.tsx
+++ b/frontend/src/components/areas/finance/AccountsTab.tsx
@@ -6,7 +6,7 @@ import { AccountManager } from './AccountManager'
 import { CategoryManager } from './CategoryManager'
 import { InvestmentsTab } from './InvestmentsTab'
 import { LoansTab } from './LoansTab'
-import { PageToolbar } from '@/components/layout/PageLayout'
+import { AreaToolbar } from '@/components/ui/AreaToolbar'
 import { AccountsTabModal } from './QuickAddAccounts'
 import { TextTabs } from '@/components/ui/TextTabs'
 import styled from 'styled-components'
@@ -39,13 +39,19 @@ export function AccountsTab() {
   return (
     <>
       <WorkspaceLayout rail={undefined}>
-        <div style={{ paddingBottom: '16px' }}>
-          <TextTabs
-            options={['Account', 'Category', 'Investment', 'Loan']}
-            value={activeTab}
-            onChange={(val) => setActiveTab(val as any)}
-          />
-        </div>
+        <AreaToolbar
+          left={
+            <TextTabs
+              options={['Account', 'Category', 'Investment', 'Loan']}
+              value={activeTab}
+              onChange={(val) => setActiveTab(val as any)}
+            />
+          }
+        >
+          <Button size="sm" variant="primary" onClick={() => setModalOpen(true)}>
+            <Plus size={12} style={{ marginRight: 4 }} /> Add {activeTab}
+          </Button>
+        </AreaToolbar>
         <GridContainer>
           {activeTab === 'Account' && (
             <GridItem>
diff --git a/frontend/src/components/areas/finance/BudgetTab.tsx b/frontend/src/components/areas/finance/BudgetTab.tsx
index 20dd532..13e8c4d 100644
--- a/frontend/src/components/areas/finance/BudgetTab.tsx
+++ b/frontend/src/components/areas/finance/BudgetTab.tsx
@@ -1,4 +1,7 @@
 import { useState, useEffect } from 'react'
+import { Plus } from 'lucide-react'
+import { Button } from '@ledgr/ui'
+import { AreaToolbar } from '@/components/ui/AreaToolbar'
 import { WorkspaceLayout } from '@/components/layout/WorkspaceLayout'
 import { BudgetsTab } from './BudgetsTab'
 import { GoalsTab } from './GoalsTab'
@@ -36,13 +39,19 @@ export function BudgetTab() {
   return (
     <>
       <WorkspaceLayout rail={undefined}>
-        <div style={{ paddingBottom: '16px' }}>
-          <TextTabs
-            options={['Budget', 'Goal', 'Bill', 'Subscription']}
-            value={activeTab}
-            onChange={(val) => setActiveTab(val as any)}
-          />
-        </div>
+        <AreaToolbar
+          left={
+            <TextTabs
+              options={['Budget', 'Goal', 'Bill', 'Subscription']}
+              value={activeTab}
+              onChange={(val) => setActiveTab(val as any)}
+            />
+          }
+        >
+          <Button size="sm" variant="primary" onClick={() => setModalOpen(true)}>
+            <Plus size={12} style={{ marginRight: 4 }} /> Add {activeTab}
+          </Button>
+        </AreaToolbar>
         <GridContainer>
           {activeTab === 'Budget' && (
             <GridItem>
diff --git a/frontend/src/components/areas/finance/TransactionsTab.tsx b/frontend/src/components/areas/finance/TransactionsTab.tsx
index 079bc19..871f0fb 100644
--- a/frontend/src/components/areas/finance/TransactionsTab.tsx
+++ b/frontend/src/components/areas/finance/TransactionsTab.tsx
@@ -810,6 +810,10 @@ export function TransactionsTab() {
         <UploadIcon size={13} />
         Import
       </ToolbarIconBtn>
+      <ToolbarDivider />
+      <Button size="sm" variant="primary" onClick={() => openAdd('Expense')}>
+        <Plus size={12} style={{ marginRight: 4 }} /> Add Transaction
+      </Button>
     </AreaToolbar>
   )
 
diff --git a/frontend/src/components/areas/health/BodySleepTab.tsx b/frontend/src/components/areas/health/BodySleepTab.tsx
index 56ec098..4791132 100644
--- a/frontend/src/components/areas/health/BodySleepTab.tsx
+++ b/frontend/src/components/areas/health/BodySleepTab.tsx
@@ -3,7 +3,7 @@ import { useState, useMemo, useEffect } from 'react'
 import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query'
 import { toast } from 'sonner'
 import { Button, Input, Select, Dialog, SegmentedControl } from '@ledgr/ui'
-import { Scale, Percent, Ruler, Moon, Clock } from 'lucide-react'
+import { Scale, Percent, Ruler, Moon, Clock, Plus } from 'lucide-react'
 import { healthApi } from '@/api/areas'
 import { Skeleton } from '@/components/ui/skeleton'
 import { format } from 'date-fns'
@@ -286,6 +286,13 @@ export function BodySleepTab() {
   return (
     <>
     <WorkspaceLayout rail={undefined}>
+      <TabToolbar
+        actions={
+          <Button size="sm" variant="primary" onClick={() => setLogModalOpen(true)}>
+            <Plus size={12} style={{ marginRight: 4 }} /> Log Body Stats / Sleep
+          </Button>
+        }
+      />
       <StyledContainer>
         <StyledKpiGrid>
           <KpiCard label="Weight" icon={Scale} color="primary" loading={loadingWeight} value={latestWeight != null ? `${latestWeight} kg` : '—'} />
diff --git a/frontend/src/components/areas/health/FitnessTab.tsx b/frontend/src/components/areas/health/FitnessTab.tsx
index 64c50f0..31e124b 100644
--- a/frontend/src/components/areas/health/FitnessTab.tsx
+++ b/frontend/src/components/areas/health/FitnessTab.tsx
@@ -777,6 +777,13 @@ export function FitnessTab() {
   return (
     <>
     <WorkspaceLayout rail={undefined}>
+      <TabToolbar
+        actions={
+          <Button size="sm" variant="primary" onClick={() => setLogModalOpen(true)}>
+            <Plus size={12} style={{ marginRight: 4 }} /> Log Workout
+          </Button>
+        }
+      />
       <StyledContainer>
         {/* Goals */}
         <div>
diff --git a/frontend/src/components/areas/health/NutritionTab.tsx b/frontend/src/components/areas/health/NutritionTab.tsx
index 2eb7fb4..c5a8f2e 100644
--- a/frontend/src/components/areas/health/NutritionTab.tsx
+++ b/frontend/src/components/areas/health/NutritionTab.tsx
@@ -3,7 +3,7 @@ import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query'
 import { toast } from 'sonner'
 import styled from 'styled-components'
 
-import { Utensils, Clock, Search } from 'lucide-react'
+import { Utensils, Clock, Search, Plus } from 'lucide-react'
 import { healthApi } from '@/api/areas'
 import { Skeleton } from '@/components/ui/skeleton'
 import { format } from 'date-fns'
@@ -462,6 +462,13 @@ export function NutritionTab() {
   return (
     <>
     <WorkspaceLayout rail={undefined}>
+      <TabToolbar
+        actions={
+          <Button size="sm" variant="primary" onClick={() => setLogModalOpen(true)}>
+            <Plus size={12} style={{ marginRight: 4 }} /> Log Meal
+          </Button>
+        }
+      />
       <StyledContainer>
         {/* Calorie ring + macros */}
         <Card title="Today's Nutrition" size="md">
diff --git a/frontend/src/components/ui/AreaToolbar.tsx b/frontend/src/components/ui/AreaToolbar.tsx
index 0ccc616..fdb95d5 100644
--- a/frontend/src/components/ui/AreaToolbar.tsx
+++ b/frontend/src/components/ui/AreaToolbar.tsx
@@ -28,9 +28,11 @@ const Shell = styled.div<{ $fullWidth: boolean }>`
   display: flex;
   align-items: center;
   gap: 8px;
-  background: transparent;
-  border: none;
-  padding: 0;
+  background: ${({ theme }) => theme.color.card};
+  border: 1px solid ${({ theme }) => theme.color.border};
+  padding: 10px 12px;
+  border-radius: 16px;
+  box-shadow: ${({ theme }) => theme.shadow.xs};
   margin-bottom: 16px;
   min-height: 44px;
   overflow-x: auto;
diff --git a/frontend/src/pages/areas/BusinessPage.tsx b/frontend/src/pages/areas/BusinessPage.tsx
index 22135e0..d9c6258 100644
--- a/frontend/src/pages/areas/BusinessPage.tsx
+++ b/frontend/src/pages/areas/BusinessPage.tsx
@@ -4,7 +4,6 @@ import { useQuery } from '@tanstack/react-query'
 import { Rocket, History, Plus, Activity, TrendingUp, LayoutDashboard, Calendar, BarChart3, Bot, Search, Bell, PlusCircle } from 'lucide-react'
 import { useNavigate } from 'react-router-dom'
 import { useUIStore } from '@/stores/uiStore'
-import { FilterBar, PeriodSelect } from '@/components/ui/FilterBar'
 import { motion } from 'framer-motion'
 import styled from 'styled-components'
 import { Timeline } from 'antd'
@@ -310,9 +309,6 @@ function RunwayCalculator() {
 
 export function BusinessPage() {
   const [isLogModalOpen, setIsLogModalOpen] = useState(false)
-  const [query, setQuery] = useState('')
-  const [eventType, setEventType] = useState('all')
-  const [period, setPeriod] = useState('2026-06')
   const navigate = useNavigate()
   const { setCmdPaletteOpen, setCaptureModalOpen } = useUIStore()
   const { data: events, isLoading: loadingEvents } = useQuery({ queryKey: ['business', 'events'], queryFn: businessApi.events })
@@ -337,28 +333,6 @@ export function BusinessPage() {
       />
       <AreaTabs
         defaultActiveKey="1"
-        toolbar={
-          <FilterBar
-            search={{ value: query, onChange: setQuery, placeholder: 'Search events, milestones…' }}
-            filters={[
-              { id: 'eventType', label: 'Type', value: eventType, onChange: setEventType, options: [
-                { value: 'all', label: 'All types' },
-                { value: 'feature', label: 'Feature' },
-                { value: 'milestone', label: 'Milestone' },
-                { value: 'revenue', label: 'Revenue' },
-              ] },
-            ]}
-            period={<PeriodSelect value={period} onChange={setPeriod} />}
-            actions={
-              <Button size="sm" variant="primary" onClick={() => setIsLogModalOpen(true)}>
-                <ActionButtonContent>
-                  <Plus size={12} />
-                  <span>Log Business Event</span>
-                </ActionButtonContent>
-              </Button>
-            }
-          />
-        }
         items={[
           {
             key: '1',
@@ -406,7 +380,14 @@ export function BusinessPage() {
                   <GlassCard
                     title="Event Timeline"
                     icon={<History size={16} color="var(--muted-foreground)" />}
-                    action={null}
+                    action={
+                      <Button size="sm" onClick={() => setIsLogModalOpen(true)}>
+                        <ActionButtonContent>
+                          <Plus size={12} />
+                          <span>Log Event</span>
+                        </ActionButtonContent>
+                      </Button>
+                    }
                     hoverable
                     fadeIn="up"
                     delay={100}
diff --git a/frontend/src/pages/areas/CareerPage.tsx b/frontend/src/pages/areas/CareerPage.tsx
index 652518a..0828641 100644
--- a/frontend/src/pages/areas/CareerPage.tsx
+++ b/frontend/src/pages/areas/CareerPage.tsx
@@ -4,7 +4,6 @@ import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query'
 import { BookOpen, History, Plus, Briefcase, ExternalLink, LayoutDashboard, Map, Target, Bot, Search, Bell, PlusCircle } from 'lucide-react'
 import { useNavigate } from 'react-router-dom'
 import { useUIStore } from '@/stores/uiStore'
-import { FilterBar, PeriodSelect } from '@/components/ui/FilterBar'
 import { motion, AnimatePresence } from 'framer-motion'
 import { toast } from 'sonner'
 import styled from 'styled-components'
@@ -288,9 +287,6 @@ function CareerStat({ label, value, sub, accent }: { label: string; value: strin
 
 export function CareerPage() {
   const [isLogModalOpen, setIsLogModalOpen] = useState(false)
-  const [query, setQuery] = useState('')
-  const [stage, setStage] = useState('all')
-  const [period, setPeriod] = useState('2026-06')
   const navigate = useNavigate()
   const { setCmdPaletteOpen, setCaptureModalOpen } = useUIStore()
 
@@ -320,25 +316,6 @@ export function CareerPage() {
         />
         <AreaTabs
           defaultActiveKey="1"
-          toolbar={
-            <FilterBar
-              search={{ value: query, onChange: setQuery, placeholder: 'Search skills, roles, companies…' }}
-              filters={[
-                { id: 'stage', label: 'Stage', value: stage, onChange: setStage, options: [
-                  { value: 'all', label: 'All stages' },
-                  { value: 'applied', label: 'Applied' },
-                  { value: 'interview', label: 'Interview' },
-                  { value: 'offer', label: 'Offer' },
-                ] },
-              ]}
-              period={<PeriodSelect value={period} onChange={setPeriod} />}
-              actions={
-                <Button size="sm" variant="primary" onClick={() => setIsLogModalOpen(true)} startIcon={<Plus size={12} />}>
-                  Add Career Item
-                </Button>
-              }
-            />
-          }
           items={[
             {
               key: '1',
@@ -364,7 +341,18 @@ export function CareerPage() {
                   </GlassCard>
 
                   <TwoColGrid>
-                    <GlassCard title="Career Timeline" icon={<History size={16} />} hoverable fadeIn="up" delay={100}>
+                    <GlassCard
+                      title="Career Timeline"
+                      icon={<History size={16} />}
+                      action={
+                        <Button size="sm" onClick={() => setIsLogModalOpen(true)}>
+                          <Plus size={12} style={{ marginRight: 4 }} /> Log Milestone
+                        </Button>
+                      }
+                      hoverable
+                      fadeIn="up"
+                      delay={100}
+                    >
                       {loadingEvents ? <StyledSkeleton $height="200px" /> : events?.length ? (
                         <Timeline
                           style={{ marginTop: 16 }}
diff --git a/frontend/src/pages/areas/ContentPage.tsx b/frontend/src/pages/areas/ContentPage.tsx
index f1be883..4fe3bf6 100644
--- a/frontend/src/pages/areas/ContentPage.tsx
+++ b/frontend/src/pages/areas/ContentPage.tsx
@@ -7,7 +7,6 @@ import { toast } from 'sonner'
 import { Plus, LayoutGrid, Edit2, Calendar, Trash2, TrendingUp, Eye, MousePointerClick, WandSparkles, PenLine, Columns3, Bot, Search, Bell, PlusCircle } from 'lucide-react'
 import { useNavigate } from 'react-router-dom'
 import { useUIStore } from '@/stores/uiStore'
-import { FilterBar, PeriodSelect } from '@/components/ui/FilterBar'
 import { motion, AnimatePresence } from 'framer-motion'
 import {
   DndContext, DragOverlay, PointerSensor,
@@ -466,9 +465,6 @@ export function ContentPage() {
   const queryClient = useQueryClient()
   const [isLogModalOpen, setIsLogModalOpen] = useState(false)
   const [activeId, setActiveId] = useState<string | null>(null)
-  const [query, setQuery] = useState('')
-  const [platform, setPlatform] = useState('all')
-  const [period, setPeriod] = useState('2026-06')
   const navigate = useNavigate()
   const { setCmdPaletteOpen, setCaptureModalOpen } = useUIStore()
 
@@ -548,25 +544,6 @@ export function ContentPage() {
         />
         <AreaTabs
           defaultActiveKey="pipeline"
-          toolbar={
-            <FilterBar
-              search={{ value: query, onChange: setQuery, placeholder: 'Search ideas, drafts, posts…' }}
-              filters={[
-                { id: 'platform', label: 'Platform', value: platform, onChange: setPlatform, options: [
-                  { value: 'all', label: 'All platforms' },
-                  { value: 'twitter', label: 'Twitter / X' },
-                  { value: 'linkedin', label: 'LinkedIn' },
-                  { value: 'blog', label: 'Blog' },
-                ] },
-              ]}
-              period={<PeriodSelect value={period} onChange={setPeriod} />}
-              actions={
-                <Button variant="primary" size="sm" onClick={() => setIsLogModalOpen(true)} startIcon={<Plus size={12} />}>
-                  Capture Idea
-                </Button>
-              }
-            />
-          }
           items={[
           {
             key: 'pipeline',
@@ -589,6 +566,9 @@ export function ContentPage() {
                           </StatBlock>
                         </StatsChip>
                       )}
+                      <Button variant="primary" size="sm" onClick={() => setIsLogModalOpen(true)} startIcon={<Plus size={12} />}>
+                        Capture Idea
+                      </Button>
                     </PageToolbar>
                   </FullRow>
 
diff --git a/frontend/src/pages/areas/FinancePage.tsx b/frontend/src/pages/areas/FinancePage.tsx
index 4bea9f9..f2720d6 100644
--- a/frontend/src/pages/areas/FinancePage.tsx
+++ b/frontend/src/pages/areas/FinancePage.tsx
@@ -1,12 +1,10 @@
 import { useState } from 'react'
 import { useNavigate } from 'react-router-dom'
 import {
-  IndianRupee, LayoutDashboard, ArrowLeftRight, Wallet, PiggyBank, Plus,
+  IndianRupee, LayoutDashboard, ArrowLeftRight, Wallet, PiggyBank,
   Bot, Search, Bell, PlusCircle,
 } from 'lucide-react'
-import { Button } from '@ledgr/ui'
 import { AreaTabs } from '@/components/ui/AreaTabs'
-import { FilterBar, PeriodSelect } from '@/components/ui/FilterBar'
 import { PageHeader, ActionChip } from '@/components/layout/PageLayout'
 import { useUIStore } from '@/stores/uiStore'
 import { HomeTab } from '@/components/areas/finance/HomeTab'
@@ -31,20 +29,8 @@ const ContentWrapper = styled.div`
   max-width: 1200px;
 `
 
-const ButtonContent = styled.div`
-  display: flex;
-  align-items: center;
-  gap: 0.25rem;
-  font-size: 12px;
-  font-weight: 500;
-`
-
 export function FinancePage() {
   const [activeKey, setActiveKey] = useState('1')
-  const [query, setQuery] = useState('')
-  const [status, setStatus] = useState('all')
-  const [type, setType] = useState('all')
-  const [period, setPeriod] = useState('2026-06')
   const navigate = useNavigate()
   const { setCmdPaletteOpen, setCaptureModalOpen } = useUIStore()
 
@@ -55,20 +41,6 @@ export function FinancePage() {
     { key: '4', label: <><PiggyBank size={14} /> Budget</>, children: <BudgetTab /> },
   ]
 
-  const primary = activeKey === '3' ? (
-    <Button size="sm" variant="primary" onClick={() => window.dispatchEvent(new CustomEvent('open-new-account'))}>
-      <ButtonContent><Plus size={12} /><span>Add Financial Item</span></ButtonContent>
-    </Button>
-  ) : activeKey === '4' ? (
-    <Button size="sm" variant="primary" onClick={() => window.dispatchEvent(new CustomEvent('open-new-budget'))}>
-      <ButtonContent><Plus size={12} /><span>Add Budget Item</span></ButtonContent>
-    </Button>
-  ) : (
-    <Button size="sm" variant="primary" onClick={() => window.dispatchEvent(new CustomEvent('open-new-transaction'))}>
-      <ButtonContent><Plus size={12} /><span>Add Transaction</span></ButtonContent>
-    </Button>
-  )
-
   return (
     <PageContainer>
       <ContentWrapper>
@@ -90,27 +62,9 @@ export function FinancePage() {
           activeKey={activeKey}
           onChange={setActiveKey}
           items={items}
-          toolbar={
-            <FilterBar
-              search={{ value: query, onChange: setQuery, placeholder: 'Search client by name, account…' }}
-              filters={[
-                { id: 'status', label: 'Status', value: status, onChange: setStatus, options: [
-                  { value: 'all', label: 'All statuses' },
-                  { value: 'cleared', label: 'Cleared' },
-                  { value: 'pending', label: 'Pending' },
-                ] },
-                { id: 'type', label: 'Type', value: type, onChange: setType, options: [
-                  { value: 'all', label: 'All types' },
-                  { value: 'income', label: 'Income' },
-                  { value: 'expense', label: 'Expense' },
-                ] },
-              ]}
-              period={<PeriodSelect value={period} onChange={setPeriod} />}
-              actions={primary}
-            />
-          }
         />
       </ContentWrapper>
     </PageContainer>
   )
 }
+
diff --git a/frontend/src/pages/areas/HealthPage.tsx b/frontend/src/pages/areas/HealthPage.tsx
index bd2ed8f..746682b 100644
--- a/frontend/src/pages/areas/HealthPage.tsx
+++ b/frontend/src/pages/areas/HealthPage.tsx
@@ -2,10 +2,9 @@
 import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query'
 import { useState, useMemo } from 'react'
 import { toast } from 'sonner'
-import { Scale, Plus, Flame, Trophy, Activity, Target, Zap, Heart, LayoutDashboard, Moon, Apple, Dumbbell, History, Bot, Search, Bell, PlusCircle } from 'lucide-react'
+import { Scale, Flame, Trophy, Activity, Target, Zap, Heart, LayoutDashboard, Moon, Apple, Dumbbell, History, Bot, Search, Bell, PlusCircle } from 'lucide-react'
 import { useNavigate } from 'react-router-dom'
 import { useUIStore } from '@/stores/uiStore'
-import { FilterBar, PeriodSelect } from '@/components/ui/FilterBar'
 import { healthApi } from '@/api/areas'
 import { formatRelativeTime, exportToCsv } from '@/lib/utils'
 import { Skeleton } from '@/components/ui/skeleton'
@@ -15,7 +14,7 @@ import { StatusPill } from '@/components/lumina';
 import { KpiCard } from '@ledgr/ui';
 import { Card as GlassCard } from '@ledgr/ui';
 import { Card as SectionCard } from '@ledgr/ui'
-import { Badge, Button } from '@ledgr/ui'
+import { Badge } from '@ledgr/ui'
 import { AreaTabs } from '@/components/ui/AreaTabs'
 import { HistoryTab } from '@/components/areas/health/HistoryTab'
 import { NutritionTab } from '@/components/areas/health/NutritionTab'
@@ -55,14 +54,6 @@ const StyledContentWrapper = styled.div`
   max-width: 1200px;
 `;
 
-const StyledButtonContent = styled.span`
-  display: flex;
-  align-items: center;
-  gap: 0.25rem;
-  font-size: 12px;
-  font-weight: 500;
-`;
-
 const StyledDashboardGrid = styled.div`
   display: grid;
   grid-template-columns: repeat(12, minmax(0, 1fr));
@@ -181,9 +172,6 @@ export function HealthPage() {
 
   const [activeKey, setActiveKey] = useState('1')
   const [isLogModalOpen, setIsLogModalOpen] = useState(false)
-  const [query, setQuery] = useState('')
-  const [logType, setLogType] = useState('all')
-  const [period, setPeriod] = useState('2026-06')
   const navigate = useNavigate()
   const { setCmdPaletteOpen, setCaptureModalOpen } = useUIStore()
 
@@ -247,40 +235,6 @@ export function HealthPage() {
       <AreaTabs
         activeKey={activeKey}
         onChange={setActiveKey}
-        toolbar={
-          <FilterBar
-            search={{ value: query, onChange: setQuery, placeholder: 'Search logs, meals, workouts…' }}
-            filters={[
-              { id: 'type', label: 'Type', value: logType, onChange: setLogType, options: [
-                { value: 'all', label: 'All types' },
-                { value: 'gym', label: 'Gym' },
-                { value: 'weight', label: 'Weight' },
-                { value: 'meal', label: 'Meal' },
-                { value: 'sleep', label: 'Sleep' },
-              ] },
-            ]}
-            period={<PeriodSelect value={period} onChange={setPeriod} />}
-            actions={
-              activeKey === '2' ? (
-                <Button size="sm" variant="primary" onClick={() => window.dispatchEvent(new CustomEvent('open-new-body-sleep'))}>
-                  <StyledButtonContent><Plus size={12} /><span>Log Body Stats / Sleep</span></StyledButtonContent>
-                </Button>
-              ) : activeKey === '3' ? (
-                <Button size="sm" variant="primary" onClick={() => window.dispatchEvent(new CustomEvent('open-new-nutrition'))}>
-                  <StyledButtonContent><Plus size={12} /><span>Log Meal</span></StyledButtonContent>
-                </Button>
-              ) : activeKey === '4' ? (
-                <Button size="sm" variant="primary" onClick={() => window.dispatchEvent(new CustomEvent('open-new-workout'))}>
-                  <StyledButtonContent><Plus size={12} /><span>Log Health Data</span></StyledButtonContent>
-                </Button>
-              ) : (
-                <Button size="sm" variant="primary" onClick={() => setIsLogModalOpen(true)}>
-                  <StyledButtonContent><Plus size={12} /><span>Log Health Data</span></StyledButtonContent>
-                </Button>
-              )
-            }
-          />
-        }
         items={[
         { key: '1', label: <StyledTabLabel><LayoutDashboard size={14} /> Dashboard</StyledTabLabel>, children: (
           <>
@@ -298,7 +252,7 @@ export function HealthPage() {
             <StyledGridItemKpi>
               <KpiCard label="Total Sessions" icon={Target} color="emerald" loading={loadingGym} value={Math.round(animatedSessions ?? 0)} />
             </StyledGridItemKpi>
-
+ 
             {/* Weight Progression chart */}
             <StyledGridItemMain>
               <SectionCard title="Weight Progression" action={<Badge tone="primary" size="sm" style={{ fontSize: '10px' }}>Past 30 Days</Badge>}>
