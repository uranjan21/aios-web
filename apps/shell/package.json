{
  "name": "@ct/shell",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p ../../tsconfig.json && vite build",
    "preview": "vite preview",
    "lint": "eslint src --ext ts,tsx"
  },
  "dependencies": {
    "@ct/career": "workspace:*",
    "@ct/finance": "workspace:*",
    "@ct/health": "workspace:*",
    "@ct/shared": "workspace:*",
    "@ledgr/ui": "workspace:*"
  }
}
